#include <thread>
#include <mutex>

class ThreadPool{
    //const uint processor_count = std::thread::hardware_concurrency();

    /*
    NEED:
    - Vector of threads
    - Queue of functions + wake-up signal
    - Mutex to guard queue

    */


   
};